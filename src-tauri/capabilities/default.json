{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "QuotaBar default capability. Covers every window including sponsor, which calls close_sponsor and listens for core events",
  "windows": ["main", "settings", "tos", "sponsor"],
  "permissions": [
    "core:default",
    "core:event:allow-listen",
    "core:window:allow-start-dragging"
  ]
}
