name: Release QuotaBar

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  release:
    permissions:
      contents: write
      attestations: write
      id-token: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: 'windows-latest'
            args: ''
          - platform: 'macos-latest'
            args: '--bundles dmg'

    runs-on: ${{ matrix.platform }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Rust Cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: './src-tauri -> target'

      - name: Install Tauri CLI
        run: cargo install tauri-cli --version "^2.0.0" --locked

      - name: Build & Bundle
        run: |
          cd src-tauri
          cargo tauri build ${{ matrix.args }}

      - name: Generate checksums
        shell: bash
        run: |
          cd src-tauri/target/release
          FILES=$(ls quotabar* 2>/dev/null; ls bundle/nsis/*.exe bundle/dmg/*.dmg 2>/dev/null)
          for f in $FILES; do shasum -a 256 "$f"; done > checksums-${{ matrix.platform }}.txt || \
          for f in $FILES; do sha256sum "$f"; done > checksums-${{ matrix.platform }}.txt
          cat checksums-${{ matrix.platform }}.txt

      - name: Build provenance attestation
        uses: actions/attest-build-provenance@v2
        with:
          subject-path: |
            src-tauri/target/release/quotabar*
            src-tauri/target/release/bundle/nsis/*.exe
            src-tauri/target/release/bundle/dmg/*.dmg

      - name: Upload Artifact to GitHub Release
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/')
        with:
          name: 'QuotaBar ${{ github.ref_name }}'
          body: |
            ## QuotaBar Release ${{ github.ref_name }}

            ### What's New
            - Non-intrusive floating quota bar for Windows 10/11 & macOS
            - Real-time tracking for Kimi, GLM, Codex, Claude, Gemini/Antigravity
            - Universal Custom Provider JSON engine
            - Zero telemetry, 100% local credentials storage

            ### Installation
            - **Windows**: `QuotaBar_x64-setup.exe` (installer) or `quotabar.exe` (portable)
            - **macOS**: `QuotaBar_x64.dmg` (drag to Applications)

            ### Verify
            `checksums-*.txt` contain SHA-256 hashes; build provenance attestation is on the *Attestations* tab.
          files: |
            src-tauri/target/release/quotabar.exe
            src-tauri/target/release/bundle/nsis/*.exe
            src-tauri/target/release/bundle/dmg/*.dmg
            src-tauri/target/release/checksums-*.txt
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
